---
phase: "07-ui-redesign"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/pages/AnalysisPage.tsx"
  - "src/pages/SalaryPage.tsx"
  - "src/pages/TransactionsPage.tsx"
  - "src/pages/UtilitiesPage.tsx"
autonomous: true
requirements:
  - "UI-01"
user_setup: []

must_haves:
  truths:
    - "AnalysisPage uses 60/40 column ratio (left/right)"
    - "SalaryPage uses 60/40 column ratio (left/right)"
    - "TransactionsPage uses 60/40 column ratio (left/right)"
    - "UtilitiesPage uses 60/40 column ratio (left/right)"
  artifacts:
    - path: "src/pages/AnalysisPage.tsx"
      provides: "Two-column layout with 60/40 ratio"
      contains: "lg: 7 for left column Grid"
    - path: "src/pages/SalaryPage.tsx"
      provides: "Two-column layout with 60/40 ratio"
      contains: "lg: 7 for left column Grid"
    - path: "src/pages/TransactionsPage.tsx"
      provides: "Two-column layout with 60/40 ratio"
      contains: "lg: 7 for left column Grid"
    - path: "src/pages/UtilitiesPage.tsx"
      provides: "Two-column layout with 60/40 ratio"
      contains: "lg: 7 for left column Grid"
  key_links:
    - from: "AnalysisPage"
      to: "Grid size props"
      via: "MUI Grid component"
    - from: "SalaryPage"
      to: "Grid size props"
      via: "MUI Grid component"
    - from: "TransactionsPage"
      to: "Grid size props"
      via: "MUI Grid component"
    - from: "UtilitiesPage"
      to: "Grid size props"
      via: "MUI Grid component"
---

<objective>
Apply consistent 60% left / 40% right column ratio to all two-column pages in the application.

Purpose: Provide consistent layout proportions across all pages. The left column typically contains interactive elements (filters, forms, tables) that benefit from more horizontal space.

Output: AnalysisPage, SalaryPage, TransactionsPage, and UtilitiesPage all updated to use lg:7 (left) / lg:5 (right) Grid sizes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-redesign/07-CONTEXT.md
@src/pages/AnalysisPage.tsx
@src/pages/SalaryPage.tsx
@src/pages/TransactionsPage.tsx
@src/pages/UtilitiesPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AnalysisPage column ratio</name>
  <files>src/pages/AnalysisPage.tsx</files>
  <action>
Change the MUI Grid sizes in AnalysisPage to achieve 60%/40% ratio:

Current configuration (line 34 and 44):
- Left Grid: `lg: 5` → change to `lg: 7`
- Right Grid: `lg: 7` → change to `lg: 5`

This swap gives the left column more space for the analysis tables.
  </action>
  <verify>
<automated>grep -q "lg: 7" src/pages/AnalysisPage.tsx && grep -q "lg: 5" src/pages/AnalysisPage.tsx</automated>
  </verify>
  <done>AnalysisPage uses lg:7 for left column, lg:5 for right column</done>
</task>

<task type="auto">
  <name>Task 2: Update SalaryPage column ratio</name>
  <files>src/pages/SalaryPage.tsx</files>
  <action>
Change the MUI Grid sizes in SalaryPage to achieve 60%/40% ratio:

Current configuration (line 113 and 181):
- Left Grid (YoY table): `lg: 5` → change to `lg: 7`
- Right Grid (trend chart): `lg: 7` → change to `lg: 5`

This gives the left column more space for the year-over-year comparison table.
  </action>
  <verify>
<automated>grep -q "lg: 7" src/pages/SalaryPage.tsx && grep -q "lg: 5" src/pages/SalaryPage.tsx</automated>
  </verify>
  <done>SalaryPage uses lg:7 for left column, lg:5 for right column</done>
</task>

<task type="auto">
  <name>Task 3: Update TransactionsPage column ratio</name>
  <files>src/pages/TransactionsPage.tsx</files>
  <action>
Change the MUI Grid sizes in TransactionsPage to achieve 60%/40% ratio:

Current configuration (line 127 and 229):
- Left Grid (filters): `lg: 4` → change to `lg: 7`
- Right Grid (table): `lg: 8` → change to `lg: 5`

This gives the filters more space and adjusts the table proportionally.
  </action>
  <verify>
<automated>grep -q "lg: 7" src/pages/TransactionsPage.tsx</automated>
  </verify>
  <done>TransactionsPage uses lg:7 for left column, lg:5 for right column</done>
</task>

<task type="auto">
  <name>Task 4: Update UtilitiesPage column ratio</name>
  <files>src/pages/UtilitiesPage.tsx</files>
  <action>
Change the MUI Grid sizes in UtilitiesPage to achieve 60%/40% ratio:

Current configuration (line 155 and 186):
- Left Grid: `lg: 5` → change to `lg: 7`
- Right Grid: `lg: 7` → change to `lg: 5`

Verify exact line numbers first as the Grid structure may vary.
  </action>
  <verify>
<automated>grep -q "lg: 7" src/pages/UtilitiesPage.tsx && grep -q "lg: 5" src/pages/UtilitiesPage.tsx</automated>
  </verify>
  <done>UtilitiesPage uses lg:7 for left column, lg:5 for right column</done>
</task>

</tasks>

<verification>
- [ ] Build passes: `npm run build`
- [ ] All four pages load without errors
- [ ] Column ratios visually consistent (left wider than right)
</verification>

<success_criteria>
All four two-column pages (AnalysisPage, SalaryPage, TransactionsPage, UtilitiesPage) use the same 60/40 column ratio with left column wider (lg:7) than right column (lg:5).
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-redesign/07-02-SUMMARY.md`
</output>