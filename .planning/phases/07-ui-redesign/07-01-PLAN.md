---
phase: "07-ui-redesign"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/pages/DashboardPage.tsx"
  - "src/components/dashboard/RecapCards.tsx"
autonomous: true
requirements:
  - "UI-01"
  - "UI-02"
user_setup: []

must_haves:
  truths:
    - "Left column (Balance + TransactionTable) is wider than right column (Charts)"
    - "Balance and income values display on single row without wrapping"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Two-column grid with 60/40 ratio"
      contains: "Grid size={{ xs: 12, lg: 7 }} for left, lg:5 for right"
    - path: "src/components/dashboard/RecapCards.tsx"
      provides: "Balance cards that fit large values"
      contains: "Typography with responsive font sizing"
  key_links:
    - from: "DashboardPage.tsx Grid left"
      to: "RecapCards"
      via: "import and render"
    - from: "RecapCards Typography"
      to: "large amounts"
      via: "toLocaleString rendering"
---

<objective>
Adjust the two-column dashboard layout to a 60/40 ratio and fix typography overflow for large balance/income values.

Purpose: Improve dashboard readability and prevent layout wrapping issues with large financial values.

Output: DashboardPage with 60/40 columns and RecapCards with proper typography sizing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-redesign/07-CONTEXT.md
@src/pages/DashboardPage.tsx
@src/components/dashboard/RecapCards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjust column ratio to 60/40</name>
  <files>src/pages/DashboardPage.tsx</files>
  <action>
Change the MUI Grid sizes to achieve a 60%/40% layout ratio:
- Change left Grid from `lg: 5` to `lg: 7` (60%)
- Change right Grid from `lg: 7` to `lg: 5` (40%)

This gives the left column (balance + transactions) more space to accommodate larger values.
  </action>
  <verify>
<automated>grep -q "lg: 7" src/pages/DashboardPage.tsx && grep -q "lg: 5" src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>Left column uses lg:7, right column uses lg:5 in Grid definition</done>
</task>

<task type="auto">
  <name>Task 2: Fix typography for large values</name>
  <files>src/components/dashboard/RecapCards.tsx</files>
  <action>
Resize typography in RecapCards to prevent wrapping for large amounts (> €1,000,000):

For the main summary cards (Current Balance, Total Income, Total Expenses):
- Currently line 120 uses variant="h5" — change to variant="h6" 
- Currently line 122 has no specific fontSize — add fontSize: { xs: '1.25rem', md: '1.5rem' } with lineHeight: 1.2

For monthly cards (lines 143-145):
- variant="h6" is already smaller, keep but ensure lineHeight: 1.2

Apply textOverflow: 'ellipsis' and whiteSpace: 'nowrap' on the amount Typography to force single-line with overflow handling.
  </action>
  <verify>
<automated>grep -q "textOverflow" src/components/dashboard/RecapCards.tsx || grep -q "fontSize" src/components/dashboard/RecapCards.tsx</automated>
  </verify>
  <done>Typography uses responsive sizing and textOverflow handling to prevent wrapping</done>
</task>

</tasks>

<verification>
- [ ] Build passes: `npm run build`
- [ ] Dashboard loads without errors
- [ ] Column ratio visually correct (left wider than right)
- [ ] Large balance values fit on single line
</verification>

<success_criteria>
Dashboard displays two-column layout with left column (lg:7) wider than right column (lg:5), and all RecapCards values fit on single rows without text wrapping.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-redesign/07-01-SUMMARY.md`
</output>