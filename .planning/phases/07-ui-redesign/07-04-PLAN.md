---
phase: "07-ui-redesign"
plan: "04"
type: "execute"
wave: 2
depends_on: []
files_modified:
  - "src/pages/SalaryPage.tsx"
autonomous: true
requirements:
  - "UI-01"
user_setup: []

must_haves:
  truths:
    - "Each month cell shows salary amount on first line"
    - "Each month cell shows combined deltas (percentage + value) on second line using | separator"
  artifacts:
    - path: "src/pages/SalaryPage.tsx"
      provides: "Compact salary table with 2-row per cell layout"
      contains: "separator for combined deltas"
  key_links:
    - from: "TableCell"
      to: "salary amount + deltas"
      via: "stacked Typography components"
---

<objective>
Compact the SalaryPage year-over-year comparison table to use 2 rows instead of 3 per month cell. Combine percentage and value deltas into a single line using a separator (|).

Purpose: Increase table density by reducing vertical spacing while keeping all information visible.

Output: SalaryPage table cells show salary amount line + combined deltas line.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/SalaryPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact SalaryTable deltas to single row</name>
  <files>src/pages/SalaryPage.tsx</files>
  <read_first>
src/pages/SalaryPage.tsx (lines 142-170)
  </read_first>
  <action>
In the SalaryPage YoY table cell (around lines 142-168), combine the percentage delta chip and value delta into a single row using "|" as separator. The separator character is intentionally flexible - use "|" by default (auto-detects if the user prefers different).

Current structure (3 elements stacked):
1. Line 143-145: € X,XXX.XX (Typography)
2. Lines 149-162: +X.X% (Chip with arrow icon)
3. Lines 163-165: +€ XXX.XX (Typography caption)

New structure (2 elements stacked):
1. Line 143-145: € X,XXX.XX (Typography - keep as is)
2. Lines 148-167: Combined deltas - combine into single Typography:
   - Format: "+X.X% | +€XXX" or "-X.X% | -€XXX"
   - Use light font weight for deltas
   - Color: green (#10b981) for positive, red (#ef4444) for negative
   - Remove the separate Chip and caption elements
   - Add "| " separator between percentage and value

This reduces the vertical stack from 3 elements to 2 elements per cell.
  </action>
  <acceptance_criteria>
- Salary amount displays on first line (unchanged)
- Single second line combines: percentage delta + "| " + value delta
- No Chip component used for deltas (removed)
- Total vertical elements in TableCell reduced from 3 to 2
  </acceptance_criteria>
  <verify>
<automated>grep -c "| " src/pages/SalaryPage.tsx | grep -q "[1-9]" && ! grep -q "variant=\"outlined\"" src/pages/SalaryPage.tsx</automated>
  </verify>
  <done>Salary table cell shows 2-line layout: amount on line 1, combined deltas on line 2 with | separator</done>
</task>

</tasks>

<verification>
- [ ] Build passes: `npm run build`
- [ ] SalaryPage loads without errors
- [ ] Each month cell shows amount followed by combined deltas on second line
- [ ] Table rows are more compact than before
</verification>

<success_criteria>
SalaryPage YoY table cells display salary amount on first line and combined deltas (+X.X% | +€XXX) on second line. Table row height is reduced compared to original 3-row layout.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-redesign/07-04-SUMMARY.md`
</output>