---
phase: "07-ui-redesign"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - "07-01"
files_modified:
  - "src/pages/DashboardPage.tsx"
  - "src/components/dashboard/RecapCards.tsx"
autonomous: true
requirements:
  - "UI-01"
user_setup: []

must_haves:
  truths:
    - "When clicking Current Balance, accounts detail appears in right column above Charts"
    - "Accounts detail displays as a horizontal section above Cash Flow Trend"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Dashboard layout with conditional AccountsDetail"
      contains: "AccountCard or AccountsDetail component in right column Grid"
  key_links:
    - from: "RecapCards onClick"
      to: "Dashboard state"
      via: "setAccountDetails toggle in RecapCards"
---

<objective>
Move the accounts detail (sparkline cards) to display in the right column above the Cash Flow Trend when clicking the Current Balance card.

Purpose: Improve dashboard layout by showing account details in the right column where there's more vertical space, keeping the charts visible as the header for that column.

Output: DashboardPage with conditional rendering of accounts detail in right column above Charts component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/DashboardPage.tsx
@src/components/dashboard/RecapCards.tsx
@src/components/dashboard/AccountCard.component.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move account details to right column</name>
  <files>src/pages/DashboardPage.tsx</files>
  <action>
Restructure DashboardPage to display account details above Charts in the right column:

1. Move the accountDetails state from RecapCards to DashboardPage (or pass it down as a prop)

2. Modify the right column Grid (currently lines 64-66) to conditionally render:
   - If accountDetails is true: render AccountCard components first, then Charts below
   - If accountDetails is false: render just Charts

3. Pass accountDetails state and setter to RecapCards via props (or lift state up):
   - Currently RecapCards manages the accountDetails state internally
   - Either: Lift state to DashboardPage and pass as props
   - Or: Use a callback pattern where RecapCards emits an event when clicked

4. Update RecapCards to NOT render the account details inline, but instead call the callback:
   - Remove lines 150-166 from RecapCards (the accountDetails section)
   - Add onClick handler that calls onToggleAccountDetails callback prop

This moves the sparkline cards from below RecapCards to above the Cash Flow chart in the right column when the balance is clicked.
  </action>
  <verify>
<automated>grep -q "accountDetails" src/pages/DashboardPage.tsx || grep -q "onToggleAccountDetails" src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>Account details render above Charts when Current Balance is clicked</done>
</task>

</tasks>

<verification>
- [ ] Build passes: `npm run build`
- [ ] Dashboard loads without errors
- [ ] Clicking Current Balance shows account details in right column
- [ ] Account details appear above Cash Flow Trend chart
</verification>

<success_criteria>
When clicking the Current Balance card, accounts detail (sparkline cards for each account) appears in the right column, positioned above the Cash Flow Trend chart.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-redesign/07-03-SUMMARY.md`
</output>