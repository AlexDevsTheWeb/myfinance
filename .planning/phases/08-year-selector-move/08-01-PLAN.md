---
phase: 08-year-selector-move
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AnalysisPage.tsx
  - src/pages/CarPage.tsx
  - src/pages/UtilitiesPage.tsx
autonomous: true
requirements:
  - UI-08

must_haves:
  truths:
    - "User can see page title and year selector on same row, aligned to right"
    - "Year selector functions correctly on each page after move"
  artifacts:
    - path: "src/pages/AnalysisPage.tsx"
      provides: "Page header with year selector inline"
      min_lines: 52
    - path: "src/pages/CarPage.tsx"
      provides: "Page header with year selector inline"
      min_lines: 555
    - path: "src/pages/UtilitiesPage.tsx"
      provides: "Page header with year selector inline"
      min_lines: 250
  key_links:
    - from: "AnalysisPage header Box"
      to: "YearSelector component"
      via: "justifyContent: 'space-between'"
      pattern: "space-between"
    - from: "CarPage header Box"
      to: "YearSelector"
      via: "justifyContent: 'space-between'"
      pattern: "space-between"
    - from: "UtilitiesPage header Box"
      to: "YearSelector"
      via: "justifyContent: 'space-between'"
      pattern: "space-between"
---

<objective>
Move the year selector from its current position to inline with the page title using space-between layout, creating a cleaner UI across three pages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/AnalysisPage.tsx
@src/pages/CarPage.tsx
@src/pages/UtilitiesPage.tsx

# YearSelector component location for reference:
# src/components/common/YearSelector.component.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move year selector inline in AnalysisPage</name>
  <files>src/pages/AnalysisPage.tsx</files>
  <action>
    In AnalysisPage.tsx, restructure the header section (lines 23-31):

    CURRENT STRUCTURE:
    - Box with mb:4 containing Typography h4
    - Separate Box with display:flex containing YearSelector

    CHANGE TO:
    - Single Box with mb:4 containing nested Box with justifyContent: 'space-between', alignItems: 'center'
    - The outer Box: <Box sx={{ mb: 4 }}>
    - The inner header Box: <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', width: '100%' }}>
    - Inside inner header Box: first = Typography h4 (Financial Analysis), second = YearSelector

    Keep the subtitle Typography below the space-between Box (still displays below title).
  </action>
  <verify>
    Verify: Run `npm run build` and check TypeScript compiles without errors.
  </verify>
  <done>Year selector appears on same row as page title, aligned to right side</done>
</task>

<task type="auto">
  <name>Task 2: Move year selector inline in CarPage</name>
  <files>src/pages/CarPage.tsx</files>
  <action>
    In CarPage.tsx, restructure the header section (lines 257-266):

    CURRENT STRUCTURE:
    - Box with mb:4 containing Typography h4
    - Separate Grid container with YearSelector (lines 268-276)

    CHANGE TO:
    - Merge into single Box with mb:4
    - Add inner Box with justifyContent: 'space-between', alignItems: 'center'
    - Typography h4: flexGrow: 1 (so it takes available space)
    - YearSelector in a Box wrapping the existing Grid

    Note: The existing Grid wraps YearSelector - keep the Grid but wrap it with display:flex to align properly.

    The current YearSelector wrapper at lines 268-276:
    ```
    <Grid container spacing={3}>
      <Grid size={{ xs: 12, lg: 3 }}>
        <YearSelector ... />
      </Grid>
    </Grid>
    ```

    CHANGE TO:
    ```
    <Box sx={{ display: 'flex', justifyContent: 'flex-end' }}>
      <YearSelector ... />
    </Box>
    ```

    Or directly within the space-between header Box.
  </action>
  <verify>
    Verify: Run `npm run build` and check TypeScript compiles without errors.
  </verify>
  <done>Year selector appears on same row as page title, aligned to right side</done>
</task>

<task type="auto">
  <name>Task 3: Move year selector inline in UtilitiesPage</name>
  <files>src/pages/UtilitiesPage.tsx</files>
  <action>
    In UtilitiesPage.tsx, restructure the header section (lines 211-228):

    CURRENT STRUCTURE:
    - Box with mb:4 containing Typography h4
    - Typography subtitle below title
    - Tabs section (lines 223-228)
    - Separate YearSelector in Grid (lines 230-238) - positioned BELOW tabs

    CHANGE TO:
    - Keep the Tabs section in same position (lines 223-228)
    - Move YearSelector to be inline with page title (above tabs, not below)
    - Use space-between layout in header Box
    - Page title + subtitle on left, YearSelector on right

    Actually, the request says move year selector to "page title section" - so it should be above the tabs.

    STRUCTURE:
    - Box with mb: 4 contains:
      - Box with justifyContent: 'space-between', alignItems: 'flex-start' (header row)
        - Box with title (Typography h4 + subtitle)
        - YearSelector (aligned right)
      - Tabs remain below
  </action>
  <verify>
    Verify: Run `npm run build` and check TypeScript compiles without errors.
  </verify>
  <done>Year selector appears on same row as page title, above tabs, aligned to right side</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- npm run build passes without TypeScript errors
- All three pages render correctly with year selector inline with title
- Year selector functionality preserved (year change updates data correctly)
</verification>

<success_criteria>
- Year selector inline with page title on AnalysisPage
- Year selector inline with page title on CarPage  
- Year selector inline with page title on UtilitiesPage
- All three selectors aligned to the right side using space-between
- No visual regression - same look and feel
</success_criteria>

<output>
After completion, create `.planning/phases/08-year-selector-move/08-01-SUMMARY.md`
</output>