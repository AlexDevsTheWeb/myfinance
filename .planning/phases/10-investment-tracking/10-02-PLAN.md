---
phase: 10-investment-tracking
plan: '02'
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/store/useInvestmentStore.ts
  - src/store/validation/investment.validation.ts
  - src/store/validation/index.ts
  - src/store/sanitization/investment.ts
  - src/store/sanitization/index.ts
  - src/hooks/useInvestmentSync.ts
autonomous: true
requirements:
  - D-01
  - D-03
  - D-07
  - D-10
  - D-11
must_haves:
  truths:
    - Investment store exists as standalone Zustand store with etfTransactions, portfolioSnapshots, and brokerConfig state
    - Store actions exist for addEtfTransaction, updateEtfTransaction, deleteEtfTransaction, setBrokerConfig, addPortfolioSnapshot
    - All CRUD actions follow optimistic update pattern with Firestore persistence (matching useFinanceStore pattern)
    - ETF transactions are validated before being added (amount > 0, required fields present)
    - ETF transactions are sanitized before Firestore write (null coalescing for optional fields)
    - Broker config writes directly to Firestore user document
    - Portfolio snapshots are auto-recorded after ETF transaction add
    - Accrued interest calculation utility returns correct monthly amount: cashBalance * (rate / 100) / 12
    - Firestore sync hook initializes investment store from user document on app start
  artifacts:
    - path: src/store/useInvestmentStore.ts
      provides: Complete Zustand store for investment domain
      contains: ETF transaction CRUD, broker config management, portfolio snapshot management
    - path: src/store/validation/investment.validation.ts
      provides: ETF transaction and broker config validation functions
      contains: validateEtfTransaction returning { valid, error? }
    - path: src/store/validation/index.ts (edited)
      provides: Barrel re-export of investment validation functions
      contains: export { validateEtfTransaction }
    - path: src/store/sanitization/investment.ts
      provides: Firestore-safe serialization for ETF transactions and broker config
      contains: sanitizeEtfTransaction, sanitizeBrokerConfig
    - path: src/store/sanitization/index.ts (edited)
      provides: Barrel re-export of investment sanitizers
      contains: export { sanitizeEtfTransaction, sanitizeBrokerConfig }
    - path: src/hooks/useInvestmentSync.ts
      provides: Firestore init + realtime sync for investment store
      contains: Firestore onSnapshot subscription for user document investment fields
  key_links:
    - from: useInvestmentStore.ts
      to: Firestore /users/{uid} document
      via: doc(db, 'users', userId) + updateDoc
      pattern: updateDoc(docRef, { etfTransactions: sanitized })
    - from: useInvestmentSync.ts
      to: useInvestmentStore.ts
      via: setAll() call on snapshot
      pattern: useInvestmentStore.getState().setAll({ ... })
    - from: useInvestmentStore.ts
      to: investment.validation.ts
      via: import + validateEtfTransaction() call before optimistic update
      pattern: const validation = validateEtfTransaction(tx); if (!validation.valid) { ... }
---

<objective>
Create the standalone Zustand investment store with full CRUD actions, validation and sanitization helpers for ETF transactions and broker config, and a Firestore sync hook that initializes and keeps investment data in sync.

Purpose: Establish the runtime data layer for all investment operations. Every subsequent plan (broker modal, transaction flow, portfolio page, market data) reads from or writes through this store.

Output:
- `src/store/useInvestmentStore.ts` — Zustand store (etfTransactions, portfolioSnapshots, brokerConfig state + CRUD)
- `src/store/validation/investment.validation.ts` — ETF transaction validation function
- `src/store/validation/index.ts` — Updated barrel export
- `src/store/sanitization/investment.ts` — Firestore sanitization functions
- `src/store/sanitization/index.ts` — Updated barrel export
- `src/hooks/useInvestmentSync.ts` — Firestore sync subscription hook
</objective>

<execution_context>
@/Users/abstract/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/abstract/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-investment-tracking/10-01-PLAN.md
@.planning/phases/10-investment-tracking/10-CONTEXT.md
@.planning/phases/10-investment-tracking/10-RESEARCH.md

# Reference patterns for store, validation, sanitization, and sync
@src/store/useFinanceStore.ts
@src/store/useAuthStore.ts
@src/store/validation/finance.validation.ts
@src/store/validation/index.ts
@src/store/sanitization/transaction.ts
@src/store/sanitization/index.ts
@src/hooks/useSyncFinance.ts

# Type dependencies (created in Plan 10-01)
@src/store/types/investment.types.ts
@src/store/types/finance.types.ts
@src/store/defaults.ts
@src/lib/converters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create investment validation and sanitization modules</name>
  <files>src/store/validation/investment.validation.ts, src/store/validation/index.ts, src/store/sanitization/investment.ts, src/store/sanitization/index.ts</files>
  <read_first>
  - src/store/validation/finance.validation.ts — validateTransaction function signature and return type pattern
  - src/store/validation/index.ts — barrel re-export pattern
  - src/store/sanitization/transaction.ts — sanitizeTransaction function pattern with null coalescing
  - src/store/sanitization/index.ts — barrel re-export pattern
  </read_first>
  <action>
Create and edit four files:

(1) Create src/store/validation/investment.validation.ts with:
```
import type { IETFTransaction, IBrokerConfig } from '../types/investment.types';

export function validateEtfTransaction(tx: IETFTransaction): { valid: boolean; error?: string } {
  if (!tx.ticker?.trim()) {
    return { valid: false, error: 'Ticker symbol is required' };
  }
  if (typeof tx.units !== 'number' || tx.units <= 0) {
    return { valid: false, error: 'Units must be greater than 0' };
  }
  if (typeof tx.price !== 'number' || tx.price <= 0) {
    return { valid: false, error: 'Price must be greater than 0' };
  }
  if (!tx.date) {
    return { valid: false, error: 'Date is required' };
  }
  if (!tx.accountId) {
    return { valid: false, error: 'Account is required' };
  }
  return { valid: true };
}

export function validateBrokerConfig(config: IBrokerConfig): { valid: boolean; error?: string } {
  if (!config.brokerName?.trim()) {
    return { valid: false, error: 'Broker name is required' };
  }
  if (!config.ticker?.trim()) {
    return { valid: false, error: 'Ticker symbol is required' };
  }
  if (typeof config.monthlyPacAmount !== 'number' || config.monthlyPacAmount < 0) {
    return { valid: false, error: 'PAC amount must be 0 or greater' };
  }
  if (typeof config.interestRate !== 'number' || config.interestRate < 0 || config.interestRate > 100) {
    return { valid: false, error: 'Interest rate must be between 0 and 100' };
  }
  return { valid: true };
}
```

(2) Edit src/store/validation/index.ts — add line after existing exports:
```
export {
  validateEtfTransaction,
  validateBrokerConfig,
} from './investment.validation';
```

(3) Create src/store/sanitization/investment.ts with:
```
import type { IETFTransaction, IBrokerConfig } from '../types/investment.types';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const sanitizeEtfTransaction = (tx: IETFTransaction): any => {
  return {
    id: tx.id,
    date: tx.date,
    ticker: tx.ticker.toUpperCase(),
    description: tx.description ?? '',
    type: tx.type,
    units: Number(tx.units),
    price: Number(tx.price),
    totalAmount: Number(tx.totalAmount) || Number(tx.units) * Number(tx.price),
    accountId: tx.accountId,
    notes: tx.notes ?? null,
  };
};

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const sanitizeBrokerConfig = (config: IBrokerConfig): any => {
  return {
    brokerName: config.brokerName,
    lumpSumAmount: Number(config.lumpSumAmount) || 0,
    monthlyPacAmount: Number(config.monthlyPacAmount) || 0,
    ticker: config.ticker.toUpperCase(),
    interestRate: Number(config.interestRate) || 0,
  };
};
```

(4) Edit src/store/sanitization/index.ts — add after existing exports:
```
export { sanitizeEtfTransaction, sanitizeBrokerConfig } from './investment';
```
  </action>
  <acceptance_criteria>
- [ ] src/store/validation/investment.validation.ts exists with validateEtfTransaction and validateBrokerConfig
- [ ] Validation returns { valid: false, error: string } for missing ticker, units <= 0, price <= 0
- [ ] Validation returns { valid: true } for valid ETF transaction
- [ ] validation/index.ts exports validateEtfTransaction and validateBrokerConfig
- [ ] src/store/sanitization/investment.ts exists with sanitizeEtfTransaction and sanitizeBrokerConfig
- [ ] sanitizeEtfTransaction converts ticker to uppercase, null-coalesces optional fields
- [ ] sanitizeBrokerConfig converts ticker to uppercase, preserves all config fields
- [ ] sanitization/index.ts exports sanitizeEtfTransaction and sanitizeBrokerConfig
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create useInvestmentStore.ts Zustand store</name>
  <files>src/store/useInvestmentStore.ts</files>
  <read_first>
  - src/store/useFinanceStore.ts — optimistic update pattern for addTransaction/updateTransaction/deleteTransaction (lines 161-256)
  - src/store/useAuthStore.ts — standalone store pattern
  - src/store/types/investment.types.ts — type references needed for store
  - src/store/defaults.ts — DEFAULT_BROKER_CONFIG for initial state
  </read_first>
  <action>
Create src/store/useInvestmentStore.ts following the existing Zustand store patterns:

- Imports: dayjs, create from zustand, doc/updateDoc from firebase/firestore, db from lib/firebase, useAuthStore, Types from ./types (for IETFTransaction, IPortfolioSnapshot, IBrokerConfig), Validation from ./validation (validateEtfTransaction, validateBrokerConfig), Sanitization from ./sanitization (sanitizeEtfTransaction, sanitizeBrokerConfig), Defaults from ./defaults (DEFAULT_BROKER_CONFIG)

- Interface InvestmentState with:
  - etfTransactions: IETFTransaction[]
  - portfolioSnapshots: IPortfolioSnapshot[]
  - brokerConfig: IBrokerConfig
  - currentPrice: number | null (cached ETF price, for live display)
  - lastPriceUpdate: string | null (ISO timestamp of last price fetch)
  - isSaving: boolean
  - saveError: string | null

- Actions (all async Firestore CRUD matching useFinanceStore pattern):
  - addEtfTransaction: validate → optimistic push to etfTransactions[] → updateDoc to Firestore → rollback on error
  - updateEtfTransaction: validate → optimistic map → updateDoc → rollback on error
  - deleteEtfTransaction: optimistic filter → updateDoc → rollback on error
  - setBrokerConfig: validate → set state → updateDoc → rollback on error
  - addPortfolioSnapshot: validate → optimistic push → updateDoc → rollback on error
  - setCurrentPrice: (price: number) => void — set currentPrice + lastPriceUpdate
  - setAll: (data: Partial<InvestmentState>) => void — bulk set from Firestore sync (does NOT persist)
  - clearSaveError: () => void

Optimistic update pattern for addEtfTransaction (matching useFinanceStore addTransaction lines 161-190):
1. Validate transaction — return if invalid
2. Set isSaving: true, saveError: null
3. Optimistically update state (sort by date desc)
4. Persist entire etfTransactions array to Firestore via updateDoc
5. On error: revert by filtering out the new ID, set saveError

For sorting ETF transactions, use: .sort((a, b) => dayjs(b.date).unix() - dayjs(a.date).unix())

For Firestore persistence, write directly to the user document fields:
- updateDoc(docRef, { etfTransactions: sanitizedTxs }) for ETF transaction changes
- updateDoc(docRef, { brokerConfig: sanitizedConfig }) for config changes
- updateDoc(docRef, { portfolioSnapshots: sanitizedSnapshots }) for snapshot changes

After addEtfTransaction success, automatically call addPortfolioSnapshot to record the updated portfolio state. The snapshot calculation:
- totalInvested: sum of all buy transactions' totalAmount minus sell transactions' totalAmount (cost basis)
- currentValue: use existing currentPrice * totalUnits
- cashBalance: read from brokerConfig (or previous snapshot value)
- accruedInterest: use the calcAccruedInterest utility
- holdings: aggregate by ticker, calculate avgCost using PMC formula

Include a utility function (separate from store actions, exported):
```
export function calcAccruedInterest(cashBalance: number, annualRate: number): number {
  return cashBalance * (annualRate / 100) / 12;
}
```

The store initial state:
- etfTransactions: []
- portfolioSnapshots: []
- brokerConfig: Defaults.DEFAULT_BROKER_CONFIG
- currentPrice: null
- lastPriceUpdate: null
- isSaving: false
- saveError: null
  </action>
  <acceptance_criteria>
- [ ] src/store/useInvestmentStore.ts exists with create<InvestmentState>() call
- [ ] Store has etfTransactions, portfolioSnapshots, brokerConfig, currentPrice, lastPriceUpdate, isSaving, saveError state
- [ ] addEtfTransaction validates, optimistically updates, persists to Firestore
- [ ] updateEtfTransaction and deleteEtfTransaction follow the same optimistic + rollback pattern
- [ ] setBrokerConfig validates, sets state, persists to Firestore
- [ ] addPortfolioSnapshot creates snapshot with calculated totals
- [ ] setAll bulk-sets state without persisting
- [ ] clearSaveError resets saveError to null
- [ ] calcAccruedInterest exports correct monthly calculation: cashBalance * (rate / 100) / 12
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create useInvestmentSync.ts Firestore sync hook</name>
  <files>src/hooks/useInvestmentSync.ts</files>
  <read_first>
  - src/hooks/useSyncFinance.ts — full pattern: onSnapshot subscription, runTransaction init, setAll call
  - src/store/useInvestmentStore.ts — setAll action signature
  - src/lib/converters.ts — getUserDocRef function
  </read_first>
  <action>
Create src/hooks/useInvestmentSync.ts following the exact pattern from useSyncFinance.ts:

- Imports: useEffect, useRef from react; runTransaction, onSnapshot from firebase/firestore; db from lib/firebase; getDefaultUserConfig from store/sync; getUserDocRef from lib/converters; useAuthStore from store/useAuthStore; useInvestmentStore from store/useInvestmentStore

- Export function useInvestmentSync() with:
  - Read user from useAuthStore
  - Read setAll from useInvestmentStore
  - isInitializing ref
  - useEffect that triggers on [user, setAll]:
    - If no user, reset isInitializing and return
    - If already initializing, return (debounce)
    - Get docRef via getUserDocRef(user.uid)
    - Initialize: runTransaction to get or create user doc → call setAll with investment fields
    - Subscribe to onSnapshot for real-time updates
      - Skip pending writes (hasPendingWrites)
      - Skip if isSaving or hasLocalChanges (to avoid overwriting optimistic updates)
      - Call setAll with snapshot data's investment fields
    - Return unsubscribe function

The key difference from useSyncFinance: this hook only sets investment-related fields (etfTransactions, portfolioSnapshots, brokerConfig) from the snapshot data, not all finance fields. Extract the three fields from the snapshot data and pass to setAll:
```
setAll({
  etfTransactions: data.etfTransactions ?? [],
  portfolioSnapshots: data.portfolioSnapshots ?? [],
  brokerConfig: data.brokerConfig ?? Defaults.DEFAULT_BROKER_CONFIG,
});
```
  </action>
  <acceptance_criteria>
- [ ] src/hooks/useInvestmentSync.ts exists
- [ ] Follows same pattern as useSyncFinance.ts (runTransaction init + onSnapshot subscription)
- [ ] Only syncs investment-related fields (etfTransactions, portfolioSnapshots, brokerConfig)
- [ ] Skips pending writes to avoid overwriting optimistic updates
- [ ] Returns unsubscribe function for cleanup
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Investment store writes ETF transactions, snapshots, and broker config to Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | useInvestmentStore addEtfTransaction | medium | mitigate | Validation before write via validateEtfTransaction — same pattern as validateTransaction in useFinanceStore |
| T-10-05 | Tampering | useInvestmentStore setBrokerConfig | low | mitigate | Validation via validateBrokerConfig before Firestore write |
| T-10-06 | Denial of Service | Firestore optimistic update rollback | low | accept | Rollback filters by ID which is always unique; risk of duplicated state on partial failure is minimal |
| T-10-SC | Tampering | npm installs | high | mitigate | No new npm packages in this plan — all dependencies pre-verified |
</threat_model>

<verification>
1. Run `npm run build` to verify TypeScript compilation
2. Verify store actions compile with all type imports
3. Verify validation/sanitization barrel exports are correct
4. Verify hook compiles with correct Firestore imports
</verification>

<success_criteria>
- [ ] useInvestmentStore.ts compiles and exports InvestmentState interface and store
- [ ] All CRUD actions (add, update, delete ETF tx; set broker config; add portfolio snapshot) work
- [ ] Validation module exports validateEtfTransaction and validateBrokerConfig
- [ ] Sanitization module exports sanitizeEtfTransaction and sanitizeBrokerConfig
- [ ] Both validation and sanitization are re-exported from their barrel index.ts
- [ ] useInvestmentSync.ts subscribes to Firestore and initializes investment state
- [ ] calcAccruedInterest utility exported and correct
- [ ] `npm run build` passes with zero type errors
</success_criteria>

<output>
After completion, create .planning/phases/10-investment-tracking/10-02-SUMMARY.md
</output>
