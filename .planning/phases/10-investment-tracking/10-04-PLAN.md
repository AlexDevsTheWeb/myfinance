---
phase: 10-investment-tracking
plan: '04'
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - src/components/investment/BrokerSettingsModal.tsx
autonomous: true
requirements:
  - D-09
  - D-10
  - D-11
  - D-20
must_haves:
  truths:
    - Broker settings modal exists as a reusable MUI Dialog component
    - Modal contains fields: broker name, lump-sum amount, monthly PAC amount, ticker symbol, interest rate
    - Modal pre-fills current values from useInvestmentStore brokerConfig state
    - On save, modal calls setBrokerConfig to persist to Firestore
    - Interest rate is displayed with percentage notation and validated to 0-100
    - Ticker is auto-capitalized on save (stored uppercase)
    - Modal follows existing Dialog pattern from TransactionModal
  artifacts:
    - path: src/components/investment/BrokerSettingsModal.tsx
      provides: Config dialog for broker parameters
      contains: Broker name, lump sum, monthly PAC, ticker, interest rate fields
  key_links:
    - from: BrokerSettingsModal.tsx
      to: useInvestmentStore.ts
      via: brokerConfig state + setBrokerConfig action
      pattern: useInvestmentStore(state => ({ brokerConfig, setBrokerConfig }))
    - from: BrokerSettingsModal.tsx DB fields
      to: Firestore
      via: setBrokerConfig → updateDoc
      pattern: brokerConfig persisted as map field in user document
---

<objective>
Create the BrokerSettingsModal component — a MUI Dialog for configuring broker parameters (broker name, lump-sum amount, monthly PAC amount, ticker symbol, interest rate on uninvested cash). The modal reads current values from the investment store and persists changes via setBrokerConfig.

Purpose: Allow users to configure their broker account settings. These parameters drive the investment workflow (PAC amount for monthly allocation, ticker for market data lookups, interest rate for accrued interest on uninvested cash).

Output:
- `src/components/investment/BrokerSettingsModal.tsx` — Reusable MUI Dialog for broker config
</objective>

<execution_context>
@/Users/abstract/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/abstract/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-investment-tracking/10-CONTEXT.md
@.planning/phases/10-investment-tracking/10-RESEARCH.md

# Reference patterns
@src/components/modals/TransactionModal.tsx
@src/pages/ConfigPage.tsx
@src/store/useInvestmentStore.ts
@src/store/types/investment.types.ts

# Theme for consistent styling
@src/theme/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BrokerSettingsModal component</name>
  <files>src/components/investment/BrokerSettingsModal.tsx</files>
  <read_first>
  - src/components/modals/TransactionModal.tsx — Dialog pattern, slotProps, background styling, form layout
  - src/store/useInvestmentStore.ts — brokerConfig state shape and setBrokerConfig action signature
  - src/store/types/investment.types.ts — IBrokerConfig interface field details
  - src/theme/theme.ts — Dialog paper background (#1e293b secondary color for modals from TransactionModal), borderRadius: 2
  </read_first>
  <action>
Create src/components/investment/BrokerSettingsModal.tsx as a reusable MUI Dialog component:

Imports:
```
import { Box, Button, Dialog, DialogActions, DialogContent, DialogTitle, Grid, TextField, Typography } from '@mui/material';
import React, { useState } from 'react';
import { useTranslation } from 'react-i18next';
import { useInvestmentStore } from '../../store/useInvestmentStore';
import type { IBrokerConfig } from '../../store/types';
```

Interface BrokerSettingsModalProps with:
- open: boolean
- onClose: () => void

Component body:
- Read brokerConfig and setBrokerConfig from useInvestmentStore
- Initialize local form state from brokerConfig via useState:
```
const [formData, setFormData] = useState({
  brokerName: brokerConfig.brokerName,
  lumpSumAmount: brokerConfig.lumpSumAmount.toString(),
  monthlyPacAmount: brokerConfig.monthlyPacAmount.toString(),
  ticker: brokerConfig.ticker,
  interestRate: brokerConfig.interestRate.toString(),
});
```
- Use a useEffect to reset formData when modal opens (dependency: open):
```
useEffect(() => {
  if (open) {
    setFormData({
      brokerName: brokerConfig.brokerName,
      lumpSumAmount: brokerConfig.lumpSumAmount.toString(),
      monthlyPacAmount: brokerConfig.monthlyPacAmount.toString(),
      ticker: brokerConfig.ticker,
      interestRate: brokerConfig.interestRate.toString(),
    });
  }
}, [open, brokerConfig]);
```

- Handle saving:
```
const handleSave = () => {
  const config: IBrokerConfig = {
    brokerName: formData.brokerName.trim(),
    lumpSumAmount: Number(formData.lumpSumAmount) || 0,
    monthlyPacAmount: Number(formData.monthlyPacAmount) || 0,
    ticker: formData.ticker.trim().toUpperCase(),
    interestRate: Number(formData.interestRate) || 0,
  };
  setBrokerConfig(config);
  onClose();
};
```

- Render Dialog (following TransactionModal's styling):
  - Dialog open={open} onClose={onClose} fullWidth maxWidth="sm"
  - slotProps paper: background: '#1e293b' (matching TransactionModal pattern), borderRadius: 2
  - DialogTitle with icon (Settings suggested or AccountBalance icon from MUI icons) + "Broker Settings" (use t('investment.brokerSettings') or hardcode for now — translations added in Plan 10-06)
  - DialogContent with form fields in a Grid container spacing={2}:
    - brokerName: TextField fullWidth, label="Broker Name" (or use t)
    - lumpSumAmount: TextField fullWidth, type="number", label="Lump Sum (€)", slotProps input startAdornment with € symbol
    - monthlyPacAmount: TextField fullWidth, type="number", label="Monthly PAC (€)", slotProps input startAdornment with € symbol
    - ticker: TextField fullWidth, label="ETF Ticker", placeholder="e.g. SWDA.MI", helper text="Enter Yahoo Finance symbol format"
    - interestRate: TextField fullWidth, type="number", label="Interest Rate (%)", slotProps input endAdornment with % symbol, helper text="Annual percentage yield on uninvested cash"
  - DialogActions with Cancel and Save buttons (same pattern as TransactionModal)

- Create the `src/components/investment/` directory structure by using the full file path (mkdir not needed — Write tool creates intermediates).

- Use the same eslint disable at top if needed: `/* eslint-disable @typescript-eslint/no-explicit-any */`

- Default export the component.
  </action>
  <acceptance_criteria>
- [ ] src/components/investment/BrokerSettingsModal.tsx exists
- [ ] Component is a functional React component with open/onClose props
- [ ] Dialog contains five form fields: broker name, lump sum, monthly PAC, ticker, interest rate
- [ ] Form pre-fills from current brokerConfig state when opened
- [ ] onSave calls setBrokerConfig with sanitized values (ticker uppercased)
- [ ] Dialog styling matches existing modal patterns (dark background #1e293b, sharp corners)
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal form → Investment store | Broker config values submitted via form fields, validated before persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Tampering | BrokerSettingsModal form input | low | mitigate | Local state only; setBrokerConfig validates before Firestore write via validateBrokerConfig |
| T-10-10 | Tampering | Ticker symbol injection | low | mitigate | Ticker is sanitized to uppercase alphanumeric + dot only via setBrokerConfig's existing validation |
| T-10-SC | Tampering | npm installs | high | mitigate | No new npm packages in this plan |
</threat_model>

<verification>
1. Run `npm run build` to verify TypeScript compilation
2. Manually inspect the component renders correctly with all form fields
3. Verify the modal opens and pre-fills existing values
4. Verify saving calls setBrokerConfig with correctly typed values
</verification>

<success_criteria>
- [ ] BrokerSettingsModal.tsx exists and compiles
- [ ] Dialog shows all 5 broker config fields
- [ ] Form pre-fills from existing store state
- [ ] Save triggers setBrokerConfig with IBrokerConfig object
- [ ] Ticker is uppercased on save
- [ ] `npm run build` passes with zero type errors
</success_criteria>

<output>
After completion, create .planning/phases/10-investment-tracking/10-04-SUMMARY.md
</output>
