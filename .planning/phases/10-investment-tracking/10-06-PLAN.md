---
phase: 10-investment-tracking
plan: '06'
type: execute
wave: 4
depends_on:
  - 10-05
  - 10-04
files_modified:
  - src/hooks/useMarketData.ts
  - src/analytics/hooks/useNetWorth.ts
  - src/analytics/hooks/useAccountBreakdown.ts
  - src/App.tsx
  - src/components/layout/Layout.tsx
  - src/hooks/useInvestmentSync.ts
  - src/locales/en.json
  - src/locales/it.json
autonomous: true
requirements:
  - D-16
  - D-17
  - D-23
must_haves:
  truths:
    - Market data hook fetches live ETF price from yfin.dev API on demand and stores result in investment store
    - Manual refresh button on Investment page triggers price fetch
    - Prices have "delayed up to 15 min" disclaimer displayed on the page
    - Investment page is accessible via /invest route under ProtectedRoute
    - Sidebar/nav drawer includes "Investments" navigation link with icon
    - useNetWorth hook correctly excludes 'transfer' transactions from expense calculations
    - useAccountBreakdown hook correctly excludes 'transfer' transactions from expense calculations
    - Investment sync hook is called from App.tsx alongside existing useSyncFinance
    - All investment UI labels have EN and IT translation keys
  artifacts:
    - path: src/hooks/useMarketData.ts
      provides: Market data fetching hook with yfin.dev integration
      contains: fetchQuote function, useMarketData hook with refresh callback
    - path: src/analytics/hooks/useNetWorth.ts (edited)
      provides: Updated net worth calculation that correctly handles 'transfer' type
      contains: if/else logic that excludes transfers
    - path: src/analytics/hooks/useAccountBreakdown.ts (edited)
      provides: Updated account breakdown that correctly handles 'transfer' type
      contains: if/else logic that excludes transfers
    - path: src/App.tsx (edited)
      provides: /invest route added with ProtectedRoute
      contains: new Route for InvestmentPage
    - path: src/components/layout/Layout.tsx (edited)
      provides: Investments navigation link in drawer
      contains: NavItem with TrendingUp icon and "Investments" label
    - path: src/hooks/useInvestmentSync.ts (edited)
      provides: Initialization call integrated into App.tsx
      contains: useInvestmentSync() call added to App component
    - path: src/locales/en.json (edited)
      provides: ~30 investment translation keys
      contains: investment: { ... } namespace with all labels
    - path: src/locales/it.json (edited)
      provides: Matching Italian translations
      contains: investment: { ... } namespace with Italian translations
  key_links:
    - from: useMarketData.ts
      to: yfin.dev API (https://api.yfin.dev/v1/quote)
      via: fetch() with ticker symbol
      pattern: await fetch(`${YFIN_BASE}/quote?symbols=${ticker}`)
    - from: useMarketData.ts
      to: useInvestmentStore
      via: setCurrentPrice action
      pattern: setCurrentPrice(quote.regularMarketPrice)
    - from: App.tsx
      to: InvestmentPage
      via: Route path="/invest" element={<ProtectedRoute>...</ProtectedRoute>}
      pattern: import InvestmentPage from './pages/InvestmentPage'
    - from: Layout.tsx
      to: /invest route
      via: NavLink or ListItemButton with router navigate
      pattern: navigate('/invest')
    - from: en.json / it.json
      to: InvestmentPage components
      via: useTranslation() hook
      pattern: t('investment.portfolioValue')
---

<objective>
Wire up the remaining infrastructure: live market data fetching from yfin.dev API, analytics filter updates to exclude transfer transactions, route registration for /invest, navigation link in the sidebar, investment sync hook initialization in App.tsx, and all EN/IT translation keys for the investment feature.

Purpose: Complete the phase by connecting all the pieces that require cross-cutting concerns — data fetching, analytics integrity, navigation, and internationalization. After this plan, the investment feature is fully operational.

Output:
- `src/hooks/useMarketData.ts` — Market data fetch hook
- Updated analytics hooks — transfer type excluded from expense calculations
- Updated App.tsx — /invest route + useInvestmentSync initialization
- Updated Layout.tsx — Investments nav link
- Updated en.json and it.json — ~25 translation keys each
</objective>

<execution_context>
@/Users/abstract/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/abstract/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-investment-tracking/10-CONTEXT.md
@.planning/phases/10-investment-tracking/10-RESEARCH.md

# Existing source files for edits
@src/App.tsx
@src/components/layout/Layout.tsx
@src/analytics/hooks/useNetWorth.ts
@src/analytics/hooks/useAccountBreakdown.ts
@src/analytics/hooks/useCategoryBreakdown.ts
@src/locales/en.json
@src/locales/it.json

# Store types for analytics verification
@src/store/types/finance.types.ts
@src/pages/InvestmentPage.tsx
@src/hooks/useInvestmentSync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update analytics hooks to exclude transfer transactions</name>
  <files>src/analytics/hooks/useNetWorth.ts, src/analytics/hooks/useAccountBreakdown.ts</files>
  <read_first>
  - src/analytics/hooks/useNetWorth.ts (full file — lines 22-24 for type check pattern, line 37 for else-if)
  - src/analytics/hooks/useAccountBreakdown.ts (full file — lines 17-18 for type filter pattern)
  - src/store/types/finance.types.ts — verify ITransaction.type is now 'income' | 'expense' | 'transfer'
  </read_first>
  <action>
(1a) Edit src/analytics/hooks/useNetWorth.ts:

No changes needed for the type classification logic — the existing code uses:
- Line 22: `if (t.type === 'income' && isAfterBalanceStart) runningBalance += t.amount;`
- Line 23: `else if (t.type === 'expense') runningBalance -= t.amount;`

This naturally excludes 'transfer' type because:
- Transfer is not 'income' → does not add to balance
- Transfer is not 'expense' → does not subtract from balance
- Result: transfer has zero net worth impact (per D-08)

Verify with npm run build that no type errors arise from the expanded union. The TypeScript union narrowing ensures that `t.type === 'expense'` correctly doesn't match 'transfer'.

(1b) Edit src/analytics/hooks/useAccountBreakdown.ts:

The existing code on lines 17-18:
```
const income = accTx.filter(t => t.type === 'income').reduce((s, t) => s + t.amount, 0);
const expense = accTx.filter(t => t.type === 'expense').reduce((s, t) => s + t.amount, 0);
```

This correctly filters by === 'income' and === 'expense', which naturally excludes 'transfer'. No logic changes needed.

However, verify and add a comment for clarity above the filter lines:
```
// NOTE: Transfer transactions (type: 'transfer') are intentionally excluded
// from both income and expense filters. They represent asset reallocation
// with zero net worth impact (per D-08).
```

Also verify src/analytics/hooks/useCategoryBreakdown.ts — it already filters `t.type === 'expense'` (line 19), which naturally excludes transfers. No changes needed.

(1c) Run `npm run build` to verify no type errors from the expanded union type.
  </action>
  <acceptance_criteria>
- [ ] useNetWorth.ts uses if (t.type === 'income') / else if (t.type === 'expense') pattern naturally excluding 'transfer'
- [ ] useAccountBreakdown.ts uses t.type === 'expense' filter naturally excluding 'transfer'
- [ ] useCategoryBreakdown.ts uses t.type === 'expense' filter naturally excluding 'transfer' (verified)
- [ ] npm run build passes with no type errors from the expanded 'transfer' union
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create useMarketData market data fetching hook</name>
  <files>src/hooks/useMarketData.ts</files>
  <read_first>
  - src/store/useInvestmentStore.ts — setCurrentPrice action, currentPrice/ lastPriceUpdate state, brokerConfig for ticker
  - src/store/types/investment.types.ts — IBrokerConfig for ticker field
  - 10-RESEARCH.md — yfin.dev API details: https://api.yfin.dev/v1/quote?symbols=TICKER, response shape
  </read_first>
  <action>
Create src/hooks/useMarketData.ts:

Imports:
```
import { useCallback } from 'react';
import dayjs from 'dayjs';
import { useInvestmentStore } from '../store/useInvestmentStore';
```

Constants:
```
const YFIN_BASE = 'https://api.yfin.dev/v1';
```

Interface:
```
interface YfinQuote {
  symbol: string;
  regularMarketPrice: number;
  regularMarketChange?: number;
  regularMarketChangePercent?: number;
  currency?: string;
  shortName?: string;
}
```

Functions:

(2a) Export async function fetchQuote(ticker: string): Promise<YfinQuote | null>
- Fetch from `${YFIN_BASE}/quote?symbols=${ticker}`
- Handle errors: try/catch, return null on failure
- Parse response JSON
- Return the first quote from data.quotes array
- Log error to console on failure (matching existing error handling pattern)

(2b) Export function useMarketData():
- Read brokerConfig from useInvestmentStore (for ticker)
- Read setCurrentPrice from useInvestmentStore
- Return { refreshPrices, isUpdating } with:
  - refreshPrices: async () => {
      const ticker = useInvestmentStore.getState().brokerConfig.ticker;
      const quote = await fetchQuote(ticker);
      if (quote && quote.regularMarketPrice) {
        // Update store with current price and timestamp
        const store = useInvestmentStore.getState();
        store.setCurrentPrice(quote.regularMarketPrice);
      }
    }
  - isUpdating: boolean (optional, could track loading state)

(2c) Export function useLastPrice(): number | null
- Simply return useInvestmentStore(state => state.currentPrice)

Design rationale: The hook pattern allows the InvestmentPage to call useMarketData() to get the refreshPrices callback, which can be attached to a "Refresh" button. The store's setCurrentPrice updates the cache that all components read from.
  </action>
  <acceptance_criteria>
- [ ] src/hooks/useMarketData.ts exists
- [ ] fetchQuote function fetches from yfin.dev API with correct URL
- [ ] fetchQuote handles errors gracefully (returns null on failure)
- [ ] useMarketData hook returns { refreshPrices } callback
- [ ] refreshPrices calls setCurrentPrice with fetched price and sets timestamp
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire up routing, navigation, sync, and add refresh button to InvestmentPage</name>
  <files>src/App.tsx, src/components/layout/Layout.tsx, src/hooks/useInvestmentSync.ts, src/pages/InvestmentPage.tsx</files>
  <read_first>
  - src/App.tsx (full file — Route pattern lines 58-99, ProtectedRoute component, useSyncFinance import)
  - src/components/layout/Layout.tsx — nav drawer item pattern (read full file, especially nav items)
  - src/hooks/useInvestmentSync.ts — verify hook signature
  - src/pages/InvestmentPage.tsx — add refresh button and sync call
  </read_first>
  <action>
Perform four edits:

(3a) Edit src/App.tsx:
- Add import: import InvestmentPage from './pages/InvestmentPage';
- Add import: import { useInvestmentSync } from './hooks/useInvestmentSync';
- Add useInvestmentSync() call after existing useSyncFinance() on line 42:
```
const syncInvestment = useInvestmentSync();
```
(Assign to a variable so it's called — React hooks must be called at top level.)
- Add route after the /utilities route (around line 94):
```
<Route path="/invest" element={
  <ProtectedRoute>
    <InvestmentPage />
  </ProtectedRoute>
} />
```

(3b) Edit src/components/layout/Layout.tsx:
- Need to read the full file to find the nav items. The pattern from STRUCTURE.md shows nav items in the drawer. Based on the car page, there should be a ListItemButton or similar for each route.
- Add an "Investments" nav item with TrendingUp icon (import from @mui/icons-material) leading to /invest route.
- Follow the existing nav item pattern exactly (same ListItemButton styling, same Typography variant, same active route detection).

(3c) Edit src/hooks/useInvestmentSync.ts:
No structural changes needed — the hook is already correctly implemented. Verify it's used correctly in App.tsx (step 3a).

(3d) Edit src/pages/InvestmentPage.tsx:
- Add import for useMarketData hook: import { useMarketData } from '../hooks/useMarketData';
- In the component body, call: const { refreshPrices } = useMarketData();
- Update the settings/refresh button area to include a working refresh button:
  - Add a "Refresh Prices" button with Refresh icon next to the Settings button
  - OnClick: call refreshPrices()
  - Show a "Prices delayed up to 15 min" disclaimer text below the button area
- The disclaimer text can be a small Typography variant="caption" with opacity 0.5
  </action>
  <acceptance_criteria>
- [ ] App.tsx imports and uses useInvestmentSync()
- [ ] App.tsx has /invest route rendering InvestmentPage inside ProtectedRoute
- [ ] Layout.tsx has "Investments" nav item with TrendingUp icon linked to /invest
- [ ] InvestmentPage has working "Refresh Prices" button that calls refreshPrices
- [ ] InvestmentPage shows "Prices delayed up to 15 min" disclaimer
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add translation keys for investment feature</name>
  <files>src/locales/en.json, src/locales/it.json</files>
  <read_first>
  - src/locales/en.json (full file — flat key structure, existing namespace patterns)
  - src/locales/it.json (full file — matching keys as en.json)
  </read_first>
  <action>
Add a new `investment` namespace in both locale files with these keys:

In src/locales/en.json, add after the "insights" block (before the closing `}`):
```
"investment": {
    "title": "Investments",
    "subtitle": "Track ETF portfolio and broker cash",
    "cashBalance": "Cash Balance",
    "investedCapital": "Invested Capital",
    "totalInvested": "Total Invested",
    "currentValue": "Current Value",
    "totalReturn": "Total Return",
    "portfolioValue": "Portfolio Value",
    "assetAllocation": "Asset Allocation",
    "holdings": "Holdings",
    "ticker": "Ticker",
    "units": "Units",
    "avgCost": "Avg Cost",
    "currentPrice": "Current Price",
    "value": "Value",
    "return": "Return",
    "brokerSettings": "Broker Settings",
    "brokerName": "Broker Name",
    "lumpSum": "Lump Sum (€)",
    "monthlyPac": "Monthly PAC (€)",
    "tickerLabel": "ETF Ticker",
    "tickerPlaceholder": "e.g. SWDA.MI",
    "interestRate": "Interest Rate (%)",
    "accruedInterest": "Accrued Interest",
    "perMonth": "/month",
    "annualRate": "Annual rate",
    "refreshPrices": "Refresh Prices",
    "pricesDelayed": "Prices delayed up to 15 min",
    "noHoldings": "No ETF holdings yet",
    "noChartData": "No portfolio history yet",
    "timeRange1M": "1M",
    "timeRange6M": "6M",
    "timeRange1Y": "1Y",
    "timeRangeAll": "ALL",
    "settings": "Settings",
    "navInvestments": "Investments",
    "extraordinaryIncome": "Extraordinary Income",
    "internalTransfer": "Internal Transfer",
    "transfer": "Transfer",
    "newTransfer": "New Transfer",
    "addTransfer": "Add Transfer"
}
```

In src/locales/it.json, add matching Italian translations:
```
"investment": {
    "title": "Investimenti",
    "subtitle": "Monitora il portafoglio ETF e la liquidità del broker",
    "cashBalance": "Liquidità",
    "investedCapital": "Capitale Investito",
    "totalInvested": "Totale Investito",
    "currentValue": "Valore Attuale",
    "totalReturn": "Rendimento Totale",
    "portfolioValue": "Valore del Portafoglio",
    "assetAllocation": "Allocazione",
    "holdings": "Posizioni",
    "ticker": "Ticker",
    "units": "Quote",
    "avgCost": "PMC",
    "currentPrice": "Prezzo Attuale",
    "value": "Valore",
    "return": "Rendimento",
    "brokerSettings": "Impostazioni Broker",
    "brokerName": "Nome Broker",
    "lumpSum": "Capitale Iniziale (€)",
    "monthlyPac": "PAC Mensile (€)",
    "tickerLabel": "Ticker ETF",
    "tickerPlaceholder": "es. SWDA.MI",
    "interestRate": "Tasso d'Interesse (%)",
    "accruedInterest": "Interessi Maturati",
    "perMonth": "/mese",
    "annualRate": "Tasso annuale",
    "refreshPrices": "Aggiorna Prezzi",
    "pricesDelayed": "Prezzi con ritardo fino a 15 min",
    "noHoldings": "Nessuna posizione ETF",
    "noChartData": "Nessuna cronologia portafoglio",
    "timeRange1M": "1M",
    "timeRange6M": "6M",
    "timeRange1Y": "1A",
    "timeRangeAll": "TUTTO",
    "settings": "Impostazioni",
    "navInvestments": "Investimenti",
    "extraordinaryIncome": "Entrate Straordinarie",
    "internalTransfer": "Bonifico Interno",
    "transfer": "Trasferimento",
    "newTransfer": "Nuovo Trasferimento",
    "addTransfer": "Aggiungi Trasferimento"
}
```
  </action>
  <acceptance_criteria>
- [ ] en.json has investment namespace with all ~35 translation keys
- [ ] it.json has matching investment namespace with Italian translations
- [ ] Both files are valid JSON (no trailing commas)
- [ ] npm run build passes with no import errors for translation keys
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → yfin.dev API | Ticker symbol sent in URL; market data response received and stored in Zustand |
| Client → Firestore | Transfer transaction type now part of the data model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Tampering | fetchQuote ticker injection | low | mitigate | Ticker comes from brokerConfig (user's own data), not from URL params |
| T-10-14 | Information Disclosure | yfin.dev API call reveals ticker | low | accept | Ticker symbol is public information; yfin.dev requires no auth |
| T-10-15 | Denial of Service | yfin.dev rate limiting | low | accept | 40 req/s free tier adequate for manual refresh; error handled by displaying last known price |
| T-10-16 | Spoofing | Analytics transfer not excluded from net worth | medium | mitigate | useNetWorth uses === 'income' / === 'expense' checks which naturally exclude 'transfer'; TypeScript union ensures compile-time safety |
| T-10-SC | Tampering | npm installs | high | mitigate | No new npm packages in this plan |
</threat_model>

<verification>
1. Run `npm run build` to verify TypeScript compilation
2. Verify /invest route renders InvestmentPage in the browser
3. Verify nav drawer shows Investments link
4. Verify refresh button triggers price fetch
5. Verify analytics pages correctly exclude transfers
6. Verify translation keys resolve in both EN and IT
</verification>

<success_criteria>
- [ ] useMarketData.ts fetches from yfin.dev API and stores result
- [ ] Refresh button on InvestmentPage triggers price fetch
- [ ] "Prices delayed" disclaimer shown on page
- [ ] /invest route added to App.tsx with ProtectedRoute
- [ ] Layout.tsx nav has Investments link with TrendingUp icon
- [ ] useInvestmentSync called from App.tsx (or equivalent)
- [ ] useNetWorth.ts uses === checks that exclude transfer type
- [ ] useAccountBreakdown.ts uses === checks that exclude transfer type
- [ ] en.json has investment namespace with ~35 keys
- [ ] it.json has matching Italian investment translations
- [ ] `npm run build` passes with zero type errors
</success_criteria>

<output>
After completion, create .planning/phases/10-investment-tracking/10-06-SUMMARY.md
</output>
