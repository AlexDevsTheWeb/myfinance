---
phase: 10-investment-tracking
plan: '03'
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/forms/TransactionForm.tsx
  - src/components/modals/TransactionModal.tsx
  - src/store/validation/finance.validation.ts
  - src/store/defaults.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-08
  - D-16
must_haves:
  truths:
    - TransactionForm accepts 'transfer' transaction type and renders appropriate UI
    - TransactionModal supports opening in 'transfer' mode for internal transfer recording
    - 'Extraordinary Income' category is available as a default income category for income logging
    - Internal transfer transactions with type 'transfer' pass validation
    - Transfer transactions are explicitly excluded from expense calculations by type classification
  artifacts:
    - path: src/components/forms/TransactionForm.tsx (edited)
      provides: Updated form that accepts 'transfer' as a type
      contains: type prop updated to accept 'income' | 'expense' | 'transfer'
    - path: src/components/modals/TransactionModal.tsx (edited)
      provides: Updated modal that supports 'transfer' type transactions
      contains: type prop and UI updated to accept 'transfer' type
    - path: src/store/validation/finance.validation.ts (edited)
      provides: Updated validation that accepts 'transfer' type transactions
      contains: validateTransaction now works with ITransaction types including 'transfer'
  key_links:
    - from: TransactionModal.tsx
      to: TransactionForm.tsx
      via: import and type prop
      pattern: type: 'income' | 'expense' | 'transfer'
    - from: TransactionForm.tsx
      to: finance.validation.ts
      via: validateTransaction function
      pattern: validation accepts type: 'transfer'
    - from: TransactionForm.tsx
      to: useFinanceStore
      via: addTransaction action
      pattern: transaction.type === 'transfer'
---

<objective>
Update the existing transaction form and modal components to support the new 'transfer' transaction type, add the 'Extraordinary Income' default income category, and ensure 'transfer' transactions pass validation.

Purpose: Allow users to record income as "Extraordinary Income" and internal transfers to broker accounts using the existing transaction system. Transfer transactions must be classified such that they are never counted as expenses or income for analytics calculations.

Output:
- Updated TransactionForm.tsx — accepts 'transfer' type
- Updated TransactionModal.tsx — supports 'transfer' mode
- Updated finance.validation.ts — validates 'transfer' transactions
- Updated defaults.ts — Extraordinary Income category (already done in Plan 10-01 — verify)
</objective>

<execution_context>
@/Users/abstract/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/abstract/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-investment-tracking/10-CONTEXT.md
@.planning/phases/10-investment-tracking/10-RESEARCH.md

# Existing source files to modify
@src/components/forms/TransactionForm.tsx
@src/components/modals/TransactionModal.tsx
@src/store/validation/finance.validation.ts
@src/store/types/finance.types.ts

# Verify Extraordinary Income category was added
@src/store/defaults.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update finance.validation.ts to accept 'transfer' type</name>
  <files>src/store/validation/finance.validation.ts</files>
  <read_first>
  - src/store/validation/finance.validation.ts (full file — lines 1-34)
  - src/store/types/finance.types.ts — verify ITransaction.type now includes 'transfer' (changed in Plan 10-01)
  </read_first>
  <action>
Edit src/store/validation/finance.validation.ts:

The existing validateTransaction and validateRecurringTransaction functions check for description, amount, date, category, subcategory, and accountId. These validations apply equally to 'transfer' transactions. The type field is not checked in the validation — it flows through as part of the ITransaction interface which now accepts 'transfer'.

No changes needed to the validation logic itself, because:
- validateTransaction checks structural fields (description, amount, date, category, subcategory, accountId) which all apply to transfer transactions
- The type field is part of the union type and TypeScript enforces it at compile time
- Transfer transactions have the same field requirements as income/expense (they represent money movement)

Verify by running `npm run build` that no type errors occur from the union expansion.
  </action>
  <acceptance_criteria>
- [ ] validateTransaction accepts ITransaction objects with type: 'transfer'
- [ ] npm run build passes with no type errors from type union expansion
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update TransactionForm to accept 'transfer' type</name>
  <files>src/components/forms/TransactionForm.tsx</files>
  <read_first>
  - src/components/forms/TransactionForm.tsx (full file — TransactionFormProps interface line 8, type usage lines 91-93, currentCategories derived from type, descriptionOptions filter, subcategoryOptions)
  - src/store/types/finance.types.ts — verify ITransaction.type is now 'income' | 'expense' | 'transfer'
  </read_first>
  <action>
Edit src/components/forms/TransactionForm.tsx:

(1) Update the TransactionFormProps interface type prop on line 9:
Change `type: 'income' | 'expense';` to `type: 'income' | 'expense' | 'transfer';`

(2) Update the currentCategories derivation on line 93:
When type is 'transfer', the category/subcategory selection should show a simplified set. Transfer transactions use a pseudo-category "Internal Transfer" with subcategory "Bonifico" (or "Transfer"). Add this logic:
- After line 93 (`const currentCategories = type === 'income' ? incomeCategories : categories;`), add:
```
const transferCategory = { name: 'Internal Transfer', subcategories: ['Bonifico', 'Trasferimento'] };
const currentCategories = type === 'income' ? incomeCategories : type === 'transfer' ? [transferCategory] : categories;
```

(3) Update the descriptionOptions filter on line 111:
Change `t.type === type` to `t.type === type` — this already works because type includes 'transfer'.

(4) The subcategoryOptions will automatically include the transfer subcategories since they're derived from currentCategories.

(5) The amount field and all other validation already applies equally to transfer transactions.

(6) The account selector already works — user selects source account. Note: for transfers, the source account is the account money LEAVES from (e.g., main checking account). The destination is implicit (the broker account).
  </action>
  <acceptance_criteria>
- [ ] TransactionFormProps type prop accepts 'income' | 'expense' | 'transfer'
- [ ] When type is 'transfer', category list shows "Internal Transfer" category
- [ ] When type is 'transfer', subcategory list shows transfer subcategories
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update TransactionModal to support 'transfer' type</name>
  <files>src/components/modals/TransactionModal.tsx</files>
  <read_first>
  - src/components/modals/TransactionModal.tsx (full file — TransactionModalProps interface lines 8-13, type prop, dialog title text lines 91-92, handleSubmit lines 61-87, submit button disabled logic line 107)
  </read_first>
  <action>
Edit src/components/modals/TransactionModal.tsx:

(1) Update TransactionModalProps interface on line 11:
Change `type: 'income' | 'expense';` to `type: 'income' | 'expense' | 'transfer';`

(2) Update the dialog title on lines 91-92:
Change the conditional title logic to handle 'transfer':
```
<DialogTitle sx={{ fontWeight: 700 }}>
  {transaction ? 'Edit' : 'New'} {type === 'income' ? 'Income' : type === 'transfer' ? 'Transfer' : 'Expense'}
</DialogTitle>
```

(3) Update the submit button label on lines 107-109:
Change the button text logic:
```
{transaction ? 'Update' : 'Add'} {type === 'income' ? 'Income' : type === 'transfer' ? 'Transfer' : 'Expense'}
```

(4) Update the submit button color on line 106:
Change `color={type === 'income' ? 'success' : 'error'}` to handle transfer:
```
color={type === 'income' ? 'success' : type === 'transfer' ? 'info' : 'error'}
```
Use 'info' color (muted indigo) for transfers to visually distinguish from income (green) and expense (red).

(5) The handleSubmit function adds the transaction with the correct type field — TypeScript now allows 'transfer' because ITransaction.type includes it. No logic change needed.

(6) The disabled check on line 107 already validates all required fields correctly.
  </action>
  <acceptance_criteria>
- [ ] TransactionModalProps type prop accepts 'income' | 'expense' | 'transfer'
- [ ] Dialog title shows "New Transfer" when type is 'transfer'
- [ ] Submit button shows "Add Transfer" when type is 'transfer'
- [ ] Submit button uses 'info' color for transfer type
- [ ] Transfer transactions are submitted with type: 'transfer'
- [ ] npm run build passes with no type errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → TransactionModal | User fills transfer transaction details; validated before adding to store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | TransactionForm transfer subcategory | low | mitigate | Transfer category is hardcoded (not user-editable) preventing category confusion |
| T-10-08 | Information Disclosure | Transfer showing in expense analytics | medium | mitigate | Transfer transactions have type: 'transfer' which existing analytics hooks explicitly exclude via === 'expense' checks |
| T-10-SC | Tampering | npm installs | high | mitigate | No new npm packages in this plan |
</threat_model>

<verification>
1. Run `npm run build` to verify TypeScript compilation
2. Verify TransactionModal opens with transfer type
3. Verify TransactionForm shows Internal Transfer category when type is transfer
</verification>

<success_criteria>
- [ ] TransactionForm accepts 'transfer' type in its type prop
- [ ] TransactionForm shows Internal Transfer category/subcategory when type is 'transfer'
- [ ] TransactionModal supports opening in 'transfer' mode
- [ ] Dialog title reads "New Transfer" for transfer type
- [ ] Submit button labeled "Add Transfer" with info color
- [ ] validateTransaction accepts transfer type transactions
- [ ] `npm run build` passes with zero type errors
</success_criteria>

<output>
After completion, create .planning/phases/10-investment-tracking/10-03-SUMMARY.md
</output>
