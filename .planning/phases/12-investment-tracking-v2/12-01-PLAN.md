---
phase: 12-investment-tracking-v2
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/types/investment.types.ts
  - src/store/types/index.ts
  - src/store/defaults.ts
  - src/lib/converters.ts
  - src/store/useInvestmentStore.ts
  - src/hooks/useInvestmentSync.ts
  - src/store/validation/investment.validation.ts
  - src/store/validation/index.ts
  - src/store/sanitization/investment.ts
  - src/store/sanitization/index.ts
autonomous: true
requirements:
  - REQ-MULTI
must_haves:
  truths:
    - BrokerAccount and AssetHolding interfaces exist in investment.types.ts
    - IBrokerConfig is marked @deprecated with migration comment
    - UseInvestmentStore has brokerAccounts, assetHoldings, selectedBrokerId, brokerTransactions state fields
    - Store exposes addBrokerAccount, updateBrokerAccount, deleteBrokerAccount, setSelectedBroker actions
    - DEFAULT_BROKER_ACCOUNTS export in defaults.ts replaces DEFAULT_BROKER_CONFIG
    - UserDoc interface in converters.ts includes brokerAccounts and assetHoldings arrays
    - useInvestmentSync.ts includes migration logic that detects old brokerConfig and converts to BrokerAccount[]
    - Migration runs once, is idempotent, and does not re-run after successful migration
    - All TypeScript compilation passes (npm run build)
  artifacts:
    - path: src/store/types/investment.types.ts
      provides: BrokerAccount, AssetHolding interfaces; @deprecated IBrokerConfig
      contains: BrokerAccount, AssetHolding, IBrokerConfig (deprecated), IETFTransaction, IPortfolioSnapshot, IInvestmentHolding
    - path: src/store/types/index.ts (edited)
      provides: Barrel re-export of new types
      contains: export type { BrokerAccount, AssetHolding }
    - path: src/store/defaults.ts (edited)
      provides: DEFAULT_BROKER_ACCOUNTS array replacing DEFAULT_BROKER_CONFIG
      contains: DEFAULT_BROKER_ACCOUNTS, DEFAULT_BROKER_CONFIG (kept for migration)
    - path: src/lib/converters.ts (edited)
      provides: Updated UserDoc with brokerAccounts and assetHoldings arrays
      contains: brokerAccounts: BrokerAccount[], assetHoldings: AssetHolding[]
    - path: src/store/useInvestmentStore.ts (edited)
      provides: V2 store with multi-broker state and CRUD actions
      contains: brokerAccounts[], assetHoldings[], selectedBrokerId, broker CRUD actions
    - path: src/hooks/useInvestmentSync.ts (edited)
      provides: Forward-compatible migration from IBrokerConfig to BrokerAccount[]
      contains: migrateBrokerConfig() helper, run-once migration in initializeUser
    - path: src/store/validation/investment.validation.ts (edited)
      provides: validateBrokerAccount function
      contains: validateBrokerAccount returning { valid, error? }
    - path: src/store/validation/index.ts (edited)
      provides: Barrel re-export of validateBrokerAccount
      contains: export { validateBrokerAccount }
    - path: src/store/sanitization/investment.ts (edited)
      provides: sanitizeBrokerAccounts and sanitizeBrokerAccount functions
      contains: sanitizeBrokerAccounts, sanitizeBrokerAccount
    - path: src/store/sanitization/index.ts (edited)
      provides: Barrel re-export of new sanitizers
      contains: export { sanitizeBrokerAccounts, sanitizeBrokerAccount }
  key_links:
    - from: investment.types.ts
      to: useInvestmentStore.ts
      via: import type { BrokerAccount, AssetHolding }
      pattern: brokerAccounts: BrokerAccount[], assetHoldings: AssetHolding[]
    - from: defaults.ts
      to: converters.ts
      via: DEFAULT_BROKER_ACCOUNTS as fallback in toFirestore/fromFirestore
      pattern: brokerAccounts: userDoc.brokerAccounts || DEFAULT_BROKER_ACCOUNTS
    - from: useInvestmentSync.ts
      to: useInvestmentStore.ts
      via: setAll() after migration detect-and-convert
      pattern: migrateIfNeeded(data) -> setAll({ brokerAccounts: ... })
---

<objective>
Refactor the investment data layer from single-broker (IBrokerConfig) to multi-broker architecture (BrokerAccount[], AssetHolding[]). This is the foundational change that all other Phase 12 enhancements depend on (D-12).

Purpose: Establish collection-based types, update the Zustand store with multi-broker CRUD actions, implement forward-compatible data migration for existing users, and update validation/sanitization for the new schema. No UI changes yet — pure data layer.

Output:
- Updated types with BrokerAccount, AssetHolding interfaces
- Updated store with broker CRUD actions, selectedBrokerId filter state
- Migration logic in useInvestmentSync.ts for existing brokerConfig → BrokerAccount[]
- Validation + sanitization for new types
- Updated defaults, converters, barrel exports
</objective>

<execution_context>
@/Users/abstract/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/abstract/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/store/types/investment.types.ts
@src/store/types/index.ts
@src/store/defaults.ts
@src/lib/converters.ts
@src/store/useInvestmentStore.ts
@src/hooks/useInvestmentSync.ts
@src/store/validation/investment.validation.ts
@src/store/validation/index.ts
@src/store/sanitization/investment.ts
@src/store/sanitization/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BrokerAccount and AssetHolding types, update defaults and converters</name>
  <files>
    src/store/types/investment.types.ts,
    src/store/types/index.ts,
    src/store/defaults.ts,
    src/lib/converters.ts
  </files>
  <action>
    ADD BrokerAccount and AssetHolding interfaces to investment.types.ts:

    ```typescript
    export interface BrokerAccount {
      id: string;
      name: string;
      baseLumpSum: number;
      monthlyPacAmount: number;
      interestRate: number;
    }

    export interface AssetHolding {
      ticker: string;
      brokerId: string;
      units: number;
    }
    ```

    MARK IBrokerConfig as `@deprecated Use BrokerAccount[] instead. Kept for backward-compatible migration.`

    ADD barrel re-exports in types/index.ts: `BrokerAccount`, `AssetHolding`.

    ADD to defaults.ts:
    - `DEFAULT_BROKER_ACCOUNTS: BrokerAccount[]` — array containing one default account: `[{ id: 'broker-1', name: 'Trade Republic', baseLumpSum: 0, monthlyPacAmount: 0, interestRate: 0 }]`
    - Keep `DEFAULT_BROKER_CONFIG` for migration backward compat (do not remove).

    UPDATE converters.ts UserDoc interface: replace `brokerConfig: IBrokerConfig` with `brokerAccounts: BrokerAccount[]`, `assetHoldings: AssetHolding[]`. Keep `brokerConfig` as optional legacy field if needed for migration reads — add a comment noting it will be removed after all users migrate.

    UPDATE toFirestore: serialize `brokerAccounts` and `assetHoldings` arrays. Use `DEFAULT_BROKER_ACCOUNTS` as fallback.

    UPDATE fromFirestore: deserialize `brokerAccounts` and `assetHoldings` arrays, keeping the legacy brokerConfig read for migration (read both, prefer new format).

    CRITICAL: Do NOT remove the existing `brokerConfig` serialization from toFirestore/fromFirestore — it must remain for backward-compatible reads during migration window.

    The new interfaces use plain object shapes (no I-prefix) per the research convention for the V2 types. The existing I-prefixed types remain unchanged.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    BrokerAccount, AssetHolding types exist. DEFAULT_BROKER_ACCOUNTS is exported. UserDoc has new fields. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add broker CRUD actions, selectedBrokerId, brokerTransactions to store + migration in sync hook</name>
  <files>
    src/store/useInvestmentStore.ts,
    src/hooks/useInvestmentSync.ts
  </files>
  <action>
    UPDATE src/store/useInvestmentStore.ts:

    ADD to InvestmentState interface:
    - `brokerAccounts: BrokerAccount[]` (initialized to `Defaults.DEFAULT_BROKER_ACCOUNTS`)
    - `assetHoldings: AssetHolding[]` (initialized to `[]`)
    - `selectedBrokerId: string | 'all'` (initialized to `'all'`)
    - `brokerTransactions: Record<string, IETFTransaction[]>` (initialized to `{}`)

    - `pendingPacTransaction: { brokerId: string; amount: number; date: string; status: 'pending' | 'confirmed' | 'executed' } | null` (initialized to `null`)
    - `lastPacGenerationDate: string | null` (initialized to `null` — YYYY-MM format per D-04)

    ADD actions:
    - `setSelectedBroker: (id: string | 'all') => void` — sets selectedBrokerId
    - `addBrokerAccount: (account: BrokerAccount) => Promise<void>` — optimistic update pattern matching useFinanceStore.addAccount: validate → push to array → updateDoc Firestore → rollback on error
    - `updateBrokerAccount: (account: BrokerAccount) => Promise<void>` — optimistic update: find by id, replace in array, updateDoc, rollback
    - `deleteBrokerAccount: (id: string) => Promise<void>` — optimistic: filter out, updateDoc, rollback
    - `addPendingPacTransaction: (pending: { brokerId: string; amount: number; date: string; status: 'pending' }) => void`
    - `confirmPacTransaction: () => Promise<void>` — fetches current price via fetchQuote, creates IETFTransaction with description 'System-Generated Buy' (D-10), clears pending transaction, updates lastPacGenerationDate
    - `dismissPacTransaction: () => void` — clears pendingPacTransaction without creating a transaction

    UPDATE setAll to handle new fields: brokerAccounts, assetHoldings, selectedBrokerId, brokerTransactions, pendingPacTransaction, lastPacGenerationDate

    UPDATE setBrokerConfig's existing action to also write to brokerAccounts[0] for backward compat (migrates the legacy single-broker config path to keep working during transition). This is important because Plan 2's UI update hasn't happened yet — the existing BrokerSettingsModal still calls setBrokerConfig.

    IMPORTANT: Keep the existing setBrokerConfig action exactly as-is for backward compat. Add a comment noting: "Legacy single-broker setter — maps to brokerAccounts[0] and is kept for backward compat during migration."

    UPDATE src/hooks/useInvestmentSync.ts:

    ADD a `migrateBrokerConfig(data): BrokerAccount[]` function:
    ```typescript
    function migrateBrokerConfig(data: Record<string, unknown>): BrokerAccount[] {
      if (Array.isArray(data.brokerAccounts) && data.brokerAccounts.length > 0) {
        return data.brokerAccounts as BrokerAccount[]; // Already migrated
      }
      if (data.brokerConfig && !Array.isArray(data.brokerAccounts)) {
        const old = data.brokerConfig as IBrokerConfig;
        const migrated: BrokerAccount[] = [{
          id: 'broker-1',
          name: old.brokerName || 'Trade Republic',
          baseLumpSum: old.lumpSumAmount || 0,
          monthlyPacAmount: old.monthlyPacAmount || 0,
          interestRate: old.interestRate || 0,
        }];
        // Fire-and-forget migration write
        const docRef = getUserDocRef(useAuthStore.getState().user!.uid);
        updateDoc(docRef, { brokerAccounts: migrated }).catch(() => {});
        return migrated;
      }
      return Defaults.DEFAULT_BROKER_ACCOUNTS;
    }
    ```

    UPDATE initializeUser to call migrateBrokerConfig on the loaded data, then pass the result to setAll's brokerAccounts field.

    UPDATE the onSnapshot handler similarly.

    Use a `useRef` flag `migrationAttempted` to ensure migration runs only once per session (idempotent — D-02).

    RUN `npm run build` after changes to verify TypeScript compilation.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Store exposes brokerAccounts, assetHoldings, selectedBrokerId, broker CRUD actions, PAC state fields. Sync hook has migration logic. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update validation and sanitization for new types + barrel exports</name>
  <files>
    src/store/validation/investment.validation.ts,
    src/store/validation/index.ts,
    src/store/sanitization/investment.ts,
    src/store/sanitization/index.ts
  </files>
  <action>
    UPDATE src/store/validation/investment.validation.ts:

    ADD `validateBrokerAccount(account: BrokerAccount): { valid: boolean; error?: string }`:
    - name must be non-empty string
    - baseLumpSum must be number >= 0
    - monthlyPacAmount must be number >= 0
    - interestRate must be number between 0 and 100

    ALSO update the existing `validateBrokerConfig` function to accept `BrokerAccount | IBrokerConfig` (union type) for backward compat during migration.

    UPDATE src/store/sanitization/investment.ts:

    ADD `sanitizeBrokerAccount(account: BrokerAccount): Record<string, unknown>`:
    - id: string (trimmed)
    - name: string (trimmed)
    - baseLumpSum: Number(coerced) || 0
    - monthlyPacAmount: Number(coerced) || 0
    - interestRate: Number(coerced) || 0

    ADD `sanitizeBrokerAccounts(accounts: BrokerAccount[]): Record<string, unknown>[]`:
    - maps sanitizeBrokerAccount over array

    UPDATE barrel exports:
    - validation/index.ts: add `validateBrokerAccount`
    - sanitization/index.ts: add `sanitizeBrokerAccounts`, `sanitizeBrokerAccount`

    IMPORTANT: Keep all existing validation and sanitization exports.
    RUN `npm run build` to verify no breakage.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    validateBrokerAccount and sanitizeBrokerAccount functions exist. Barrel exports updated. TypeScript compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Firestore | BrokerAccount data written from client store to Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | Broker account name injection in Firestore | medium | mitigate | Sanitize broker name via sanitizeBrokerAccount (string coercion) before updateDoc |
| T-12-02 | Spoofing | Firestore brokerAccounts read by unauthenticated user | high | mitigate | Existing Firestore rules on /users/{userId} enforce isOwner check — subcollections and fields inherit this |
| T-12-03 | Tampering | Migration race: onSnapshot overwrites migrated brokerAccounts with stale data | medium | mitigate | Use migrationAttempted ref in useInvestmentSync to prevent migration running twice; setAll checks for migration in progress before overwriting |
</threat_model>

<verification>
- `npm run build` passes without errors
- All existing investment module functionality (store reads/writes) continues working
- New BrokerAccount type is importable from store/types
- DEFAULT_BROKER_ACCOUNTS provides one default account matching old DEFAULT_BROKER_CONFIG values
</verification>

<success_criteria>
1. BrokerAccount and AssetHolding types are defined and exported
2. Store has multi-broker CRUD actions, selectedBrokerId, and PAC state fields
3. useInvestmentSync.ts detects old brokerConfig and converts to BrokerAccount[] on first load
4. Validation and sanitization functions exist for BrokerAccount
5. All barrel exports are updated
6. `npm run build` passes
</success_criteria>
